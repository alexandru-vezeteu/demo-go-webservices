@startuml idm_gateway_contract
!theme plain

title IDM Gateway Contract
caption Base Path: /api/idm/auth
footer Port: 8000

interface "Auth API" {
    + POST /register
    + POST /login
    + POST /verify
    + POST /revoke
}

note right of "Auth API"
    REST Adapter for IDM gRPC Service
    Handles HTTP/JSON requests
end note

@enduml
