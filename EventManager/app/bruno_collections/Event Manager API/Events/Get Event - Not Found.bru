meta {
  name: Get Event - Not Found
  type: http
  seq: 6
}

get {
  url: {{base_url}}/events/99999
  body: none
  auth: none
}

assert {
  res.status: eq 404
}

tests {
  test("should return 404 for non-existent event", function() {
    expect(res.status).to.equal(404);
    expect(res.body.error).to.exist;
  });
}
