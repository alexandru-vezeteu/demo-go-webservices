meta {
  name: Get Event - Success
  type: http
  seq: 5
}

get {
  url: {{base_url}}/events/1
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.id: eq 1
}

tests {
  test("should retrieve event by ID", function() {
    expect(res.status).to.equal(200);
    expect(res.body.id).to.equal(1);
    expect(res.body.name).to.exist;
  });
}
