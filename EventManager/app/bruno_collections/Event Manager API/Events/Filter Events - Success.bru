meta {
  name: Filter Events - Success
  type: http
  seq: 9
}

get {
  url: {{base_url}}/events?page=1&per_page=10&min_seats=100&order_by=seats_desc
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

tests {
  test("should filter events successfully", function() {
    expect(res.status).to.equal(200);
    expect(res.body.events).to.be.an('array');
  });
}
