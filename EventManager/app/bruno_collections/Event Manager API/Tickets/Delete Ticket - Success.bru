meta {
  name: Delete Ticket - Success
  type: http
  seq: 12
}

delete {
  url: {{base_url}}/tickets/{{ticket_code}}
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

tests {
  test("should delete ticket successfully", function() {
    expect(res.status).to.equal(200);
    expect(res.body.code).to.exist;
  });
}
