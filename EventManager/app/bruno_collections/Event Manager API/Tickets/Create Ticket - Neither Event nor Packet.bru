meta {
  name: Create Ticket - Neither Event nor Packet
  type: http
  seq: 4
}

post {
  url: {{base_url}}/tickets
  body: json
  auth: none
}

body:json {
  {}
}

assert {
  res.status: eq 400
}

tests {
  test("should reject ticket without event_id or packet_id", function() {
    expect(res.status).to.equal(400);
    expect(res.body.error).to.include("must be associated with either");
  });
}
