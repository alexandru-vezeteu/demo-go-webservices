meta {
  name: Create Inclusion - Foreign Key Error (Event Not Found)
  type: http
  seq: 3
}

post {
  url: {{base_url}}/events/99999/packets/1
  body: json
  auth: none
}

body:json {
  {}
}

assert {
  res.status: eq 404
}

tests {
  test("should return 404 when event doesn't exist", function() {
    expect(res.status).to.equal(404);
    expect(res.body.error).to.exist;
  });
}
