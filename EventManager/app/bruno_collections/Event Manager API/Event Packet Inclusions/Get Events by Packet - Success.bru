meta {
  name: Get Events by Packet - Success
  type: http
  seq: 6
}

get {
  url: {{base_url}}/event-packets/1/events
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

tests {
  test("should retrieve all events for a packet", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an('array');
  });
}
