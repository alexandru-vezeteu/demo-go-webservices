meta {
  name: Create Inclusion - Success
  type: http
  seq: 1
}

post {
  url: {{base_url}}/events/1/event-packets/1
  body: json
  auth: none
}

body:json {
  {}
}

assert {
  res.status: eq 201
}

tests {
  test("should create inclusion successfully", function() {
    expect(res.status).to.equal(201);
    expect(res.body.event_id).to.equal(1);
    expect(res.body.packet_id).to.equal(1);
  });
}
