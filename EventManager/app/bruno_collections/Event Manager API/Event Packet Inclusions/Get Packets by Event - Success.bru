meta {
  name: Get Packets by Event - Success
  type: http
  seq: 5
}

get {
  url: {{base_url}}/events/1/packets
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

tests {
  test("should retrieve all packets for an event", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an('array');
  });
}
