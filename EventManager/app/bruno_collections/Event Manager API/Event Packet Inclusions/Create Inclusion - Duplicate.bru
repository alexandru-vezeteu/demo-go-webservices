meta {
  name: Create Inclusion - Duplicate
  type: http
  seq: 4
}

post {
  url: {{base_url}}/events/1/packets/1
  body: json
  auth: none
}

body:json {
  {}
}

assert {
  res.status: eq 409
}

tests {
  test("should return 409 for duplicate inclusion", function() {
    expect(res.status).to.equal(409);
    expect(res.body.error).to.exist;
  });
}

docs {
  # Run this after the successful create inclusion test
  # Tests duplicate key constraint on (event_id, packet_id)
}
